#pragma once
#include <vector>
#include <string>
#include <memory>
#include <windows.h>

typedef std::vector<std::wstring> CharBuffer;
typedef std::vector<std::vector<WORD>> AttrBuffer;
typedef std::vector<std::vector<bool>> HitMask;
